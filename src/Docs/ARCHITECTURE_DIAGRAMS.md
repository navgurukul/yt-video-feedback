# 🏗️ Architecture Diagrams

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND (React)                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │ VideoAnalyzer│  │ AnalysisResults│ │   History    │         │
│  │    Page      │  │     Page      │  │     Page     │         │
│  └──────┬───────┘  └──────┬────────┘  └──────┬───────┘         │
│         │                  │                   │                 │
│         └──────────────────┼───────────────────┘                 │
│                            │                                     │
│                    ┌───────▼────────┐                           │
│                    │  API Requests  │                           │
│                    │ (fetch/axios)  │                           │
│                    └───────┬────────┘                           │
└────────────────────────────┼──────────────────────────────────┘
                             │
                    ┌────────▼──────────┐
                    │   Vite Proxy      │
                    │  (Port 8080)      │
                    └────────┬──────────┘
                             │
┌────────────────────────────▼──────────────────────────────────┐
│                    BACKEND (Express)                           │
│                      Port 3001                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │                    Routes Layer                          │  │
│  │              server/routes/api.js                       │  │
│  │  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐              │  │
│  │  │ POST │  │ POST │  │ GET  │  │DELETE│              │  │
│  │  │/eval │  │/store│  │/hist │  │/eval │              │  │
│  │  └──┬───┘  └──┬───┘  └──┬───┘  └──┬───┘              │  │
│  └─────┼─────────┼─────────┼─────────┼────────────────────┘  │
│        │         │         │         │                        │
│  ┌─────▼─────────▼─────────▼─────────▼────────────────────┐  │
│  │                 Controllers Layer                        │  │
│  │  ┌──────────────────┐  ┌──────────────────┐           │  │
│  │  │ evaluation       │  │ database         │           │  │
│  │  │ Controller.js    │  │ Controller.js    │           │  │
│  │  └────────┬─────────┘  └────────┬─────────┘           │  │
│  └───────────┼──────────────────────┼──────────────────────┘  │
│              │                      │                          │
│  ┌───────────▼──────────────────────▼──────────────────────┐  │
│  │                  Services Layer                          │  │
│  │  ┌──────────────────┐  ┌──────────────────┐           │  │
│  │  │ gemini           │  │ database         │           │  │
│  │  │ Service.js       │  │ Service.js       │           │  │
│  │  └────────┬─────────┘  └────────┬─────────┘           │  │
│  └───────────┼──────────────────────┼──────────────────────┘  │
│              │                      │                          │
│  ┌───────────▼────────┐  ┌──────────▼─────────┐              │
│  │  Config Layer      │  │   Config Layer     │              │
│  │  gemini.js         │  │   database.js      │              │
│  │  (AI Client)       │  │   (PG Pool)        │              │
│  └────────┬───────────┘  └──────────┬─────────┘              │
└───────────┼──────────────────────────┼────────────────────────┘
            │                          │
   ┌────────▼────────┐        ┌────────▼─────────┐
   │  Google Gemini  │        │   PostgreSQL     │
   │      API        │        │    Database      │
   │   (AI Model)    │        │  (Evaluations)   │
   └─────────────────┘        └──────────────────┘
```

## Request Flow - Video Evaluation

```
User Action: Upload Video & Click "Analyze"
    │
    ▼
┌─────────────────────────────────────────────┐
│         VideoAnalyzer Component             │
│  - Validates input                          │
│  - Prepares video details & rubric          │
│  - Sends POST /evaluate request             │
└────────────────┬────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────┐
│      evaluationController.evaluateVideo     │
│  - Validates videoUrl and API key           │
│  - Calls geminiService                      │
└────────────────┬────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────┐
│   geminiService.evaluateVideoWithGemini     │
│  - Creates Gemini AI client                 │
│  - Builds prompt with video details         │
│  - Calls streaming API                      │
│  - Parses JSON response                     │
└────────────────┬────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────┐
│           Google Gemini API                 │
│  - Analyzes video content                   │
│  - Streams back evaluation                  │
│  - Returns structured JSON                  │
└────────────────┬────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────┐
│      Response flows back to frontend        │
│  - AnalysisResults displays evaluation      │
│  - User can save to database                │
└─────────────────────────────────────────────┘
```

## Request Flow - Save Evaluation

```
User Action: Click "Save Results"
    │
    ▼
┌─────────────────────────────────────────────┐
│         VideoAnalyzer Component             │
│  - Sends POST /store-evaluation request     │
│  - Includes user email, video type, data    │
└────────────────┬────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────┐
│    databaseController.storeEvaluation       │
│  - Validates required fields                │
│  - Routes by video type (concept/project)   │
└────────────────┬────────────────────────────┘
                 │
         ┌───────┴────────┐
         ▼                ▼
┌──────────────┐  ┌──────────────┐
│   Concept    │  │   Project    │
│   Evaluation │  │  Evaluation  │
└──────┬───────┘  └──────┬───────┘
       │                 │
       ▼                 ▼
┌─────────────────────────────────────────────┐
│    databaseService.storeConceptEvaluation   │
│         OR storeProjectEvaluation           │
│  - Parses evaluation data                   │
│  - Extracts scores and feedback             │
│  - Converts objects to JSON strings         │
└────────────────┬────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────┐
│         PostgreSQL Database                 │
│  - Inserts record into table                │
│  - Returns record ID                        │
└────────────────┬────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────┐
│      Success response to frontend           │
│  - Shows success toast message              │
│  - Record saved with ID                     │
└─────────────────────────────────────────────┘
```

## Request Flow - View History

```
User Action: Navigate to History Page
    │
    ▼
┌─────────────────────────────────────────────┐
│           History Component                 │
│  - Checks user authentication               │
│  - Sends GET /concept-history request       │
│  - Sends GET /project-history request       │
└────────────────┬────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────┐
│   databaseController.getConceptHistory      │
│             getProjectHistory               │
│  - Validates email parameter                │
│  - Calls database service                   │
└────────────────┬────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────┐
│   databaseService.getConceptHistory         │
│           getProjectHistory                 │
│  - Queries PostgreSQL with user email       │
│  - Orders by created_at DESC                │
│  - Returns all matching records             │
└────────────────┬────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────┐
│         PostgreSQL Database                 │
│  - Executes SELECT query                    │
│  - Returns all evaluation records           │
└────────────────┬────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────┐
│       History Component Renders             │
│  - Displays cards for each evaluation       │
│  - Shows video type, date, phase            │
│  - Click to view full details               │
└─────────────────────────────────────────────┘
```

## File Dependencies

```
server/index-new.js
    │
    ├── config/database.js
    │       └── (pg Pool)
    │
    ├── config/gemini.js
    │       └── (GoogleGenAI client)
    │
    └── routes/api.js
            │
            ├── controllers/evaluationController.js
            │       └── services/geminiService.js
            │               └── config/gemini.js
            │
            └── controllers/databaseController.js
                    └── services/databaseService.js
                            └── config/database.js
```

## Component Hierarchy (Frontend)

```
App.tsx
  │
  ├── Header.tsx
  │     └── AuthModal.tsx (conditional)
  │
  ├── Routes
  │     │
  │     ├── Index.tsx (Home)
  │     │     ├── AnimatedIntroText
  │     │     ├── AnimatedHeading
  │     │     ├── MotionWrapper
  │     │     └── Footer
  │     │
  │     ├── VideoAnalyzer.tsx
  │     │     ├── CelebrationEffect
  │     │     ├── AnimatedHeading
  │     │     ├── MotionWrapper
  │     │     └── Footer
  │     │
  │     ├── AnalysisResults.tsx
  │     │     ├── CelebrationEffect
  │     │     ├── MotionWrapper
  │     │     └── Footer
  │     │
  │     ├── History.tsx
  │     │     ├── MotionWrapper
  │     │     ├── AuthModal (conditional)
  │     │     └── Footer
  │     │
  │     └── NotFound.tsx
  │
  └── Footer.tsx
```

## Data Flow - Evaluation Object

```
1. Frontend sends:
{
  videoUrl: "gs://...",
  videoDetails: "...",
  promptbegining: "...",
  rubric: {...},
  evaluationType: "concept" | "project",
  structuredreturnedconfig: {...}
}

2. Gemini AI returns:
{
  raw: "...",
  text: "...",
  parsed: {
    "Accuracy Level": [...],
    "Ability to explain": [...]
  }
}

3. Frontend stores:
{
  userId: "...",
  userEmail: "user@example.com",
  videoUrl: "https://...",
  videoType: "concept",
  evaluationData: {
    evaluation_result: {
      accuracy: {...},
      abilityToExplain: {...}
    }
  },
  selectedPhase: "Phase1",
  selectedVideoTitle: "Home Page"
}

4. Database stores (parsed):
{
  email: "user@example.com",
  project_name: "Phase1",
  page_name: "Home Page",
  video_url: "https://...",
  concept_explanation_accuracy: 85,
  concept_explanation_feedback: '{"What could you do well?":"...","What can you do better?":"..."}',
  ability_to_explain_evaluation: "Advanced",
  ability_to_explain_feedback: '{"What could you do well?":"..."}'
}
```

## Layer Responsibilities

### Routes Layer
- ✅ Define endpoints
- ✅ Map HTTP methods
- ✅ Connect to controllers
- ❌ NO business logic
- ❌ NO data processing

### Controllers Layer
- ✅ Validate request data
- ✅ Call appropriate services
- ✅ Format responses
- ✅ Handle HTTP errors
- ❌ NO database queries
- ❌ NO AI calls

### Services Layer
- ✅ Business logic
- ✅ External API calls
- ✅ Data transformation
- ✅ Database operations
- ❌ NO request/response handling

### Config Layer
- ✅ Setup connections
- ✅ Load environment variables
- ✅ Export clients/pools
- ❌ NO business logic
